{
  "openapi": "3.0.1",
  "info": {
    "title": "API documentation",
    "version": "v1",
    "description": "API for DfE's participant induction service"
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "bearerFormat": "string",
        "type": "http",
        "scheme": "bearer"
      }
    }
  },
  "security": [
    {
      "bearerAuth": [

      ]
    }
  ],
  "servers": [
    {
      "url": "https://{defaultHost}/api/{version}",
      "variables": {
        "defaultHost": {
          "default": "ecf-dev.london.cloudapps.digital"
        },
        "version": {
          "enum": [
            "v1"
          ],
          "default": "v1"
        }
      }
    }
  ],
  "paths": {
    "/api/v1/participants/induction-progress": {
      "post": {
        "summary": "Submit induction progress for a participant",
        "operationId": "api_v1_create_ect_participant",
        "tags": [
          "induction progress"
        ],
        "security": [
          {
            "bearerAuth": [

            ]
          }
        ],
        "parameters": [

        ],
        "responses": {
          "201": {
            "description": "Successful"
          },
          "404": {
            "description": "Not Found"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    }
  }
}